# Cosmic web (PM): Zel'dovich PM in a 100 Mpc periodic box.
# Needs 64^3 for adequate PM resolution at this box size.

[simulation]
n_grid      = 64
n_particles = 64
box_length  = 100000.0   # 100 Mpc in kpc

[time]
scale_factor_range = [0.02, 1.0]  # z ~ 49 to z = 0
